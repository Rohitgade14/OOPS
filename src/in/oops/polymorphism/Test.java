package in.oops.polymorphism;

public class Test {
	
    //	public int add(int a ,int b) ;
     //public void add(int a ,int b) ;
//	   here this e.g is not methodoverloding bcuz its paramer must be diff here paramer will be same
//	  its alos not not e.g of methodOverrding bcuz in method Overrding return must be same or its object class
//	Return type must be same or covariant
	public static void main(String[] args) {
		
	}

}
//Overloading → Same method name, different parameter lists (number, type, or order of parameters). 
//Return type can be different. Happens within the same class.

//Overriding → Same method name, same parameters, but in different classes (parent & child).
//The return type must be the same (or covariant).
